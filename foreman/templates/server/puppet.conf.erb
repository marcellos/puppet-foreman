
[master]
    reports        = foreman
    external_nodes = /etc/puppet/node.rb
    node_terminus  = exec

<% scope.lookupvar("foreman::puppet::params::environments").each do |env| -%>
[<%= env %>]
    modulepath = <%= scope.lookupvar("foreman::puppet::params::modules_path") %>/<%= env %>:<%= scope.lookupvar("foreman::puppet::params::common_modules_path") %>
<% end -%>
